---
import PageLayout from '../../../layouts/PageLayout.astro';
import CTABlock from '../../../components/CTABlock.astro';

const layers = [
  { n: '01', name: 'Financial Baseline', desc: 'Revenue tracking, expense mapping, and cashflow fundamentals for independent operators.' },
  { n: '02', name: 'Pricing Architecture', desc: 'Rate setting, pricing psychology, value-based pricing frameworks, and market positioning.' },
  { n: '03', name: 'Scenario Planning', desc: 'Cashflow forecasting, growth modelling, and risk scenario analysis.' },
  { n: '04', name: 'Reporting & Review', desc: 'Monthly financial review prompts, investor-ready summary generation, and KPI tracking.' },
];

const for_who = [
  { role: 'Founders', need: 'Who need financial clarity without a CFO on payroll.' },
  { role: 'Freelancers', need: 'Who underprice themselves and don\'t track cashflow properly.' },
  { role: 'Solo Consultants', need: 'Managing irregular income and multiple revenue streams.' },
  { role: 'Side-Hustle Operators', need: 'Who need to understand their numbers before going full-time.' },
  { role: 'Small Business Owners', need: 'Who want investor-grade financial clarity at startup budget.' },
  { role: 'Independent Professionals', need: 'Who know money is their constraint but hate spreadsheets.' },
];
---

<PageLayout
  title="Financial Planning System — SeedSystems™ Signature Systems"
  description="AI-powered financial clarity system for founders and solos. 4 modules. Cashflow, pricing, forecasting, and reporting. Coming soon."
  activePath="/systems/"
>
  <!-- ── HERO ── -->
  <section class="sys-hero">
    <div class="container">
      <div class="breadcrumb">
        <a href="/">SeedSystems™</a>
        <span class="sep">→</span>
        <a href="/systems/">Signature Systems</a>
        <span class="sep">→</span>
        <span>Financial Planning</span>
      </div>
      <div class="hero-inner">
        <div class="hero-left">
          <span class="hero-tag">SS-06 · Financial Series · In Build</span>
          <h1 class="hero-h1">
            Financial<br /><em>Planning System</em>
          </h1>
          <p class="hero-sub">
            Know your numbers. Own your decisions. AI-powered financial clarity system
            for founders, solos, and independent professionals. Cashflow modelling, scenario
            planning, pricing frameworks, and investor-ready reporting.
          </p>
          <div class="hero-actions">
            <a href="https://seedsystems.co/contact/" class="btn-primary" target="_blank" rel="noopener">
              Notify Me When Live
            </a>
            <a href="/" class="btn-ghost" target="_blank" rel="noopener">
              Back to Systems
            </a>
          </div>
        </div>
        <div class="hero-right">
          <div class="hero-card">
            <div class="hc-label">Coming Soon</div>
            <div class="hc-item"><span class="hc-check">✓</span> 4 financial modules</div>
            <div class="hc-item"><span class="hc-check">✓</span> 20+ master prompts</div>
            <div class="hc-item"><span class="hc-check">✓</span> Cashflow tracking</div>
            <div class="hc-item"><span class="hc-check">✓</span> Pricing frameworks</div>
            <div class="hc-item"><span class="hc-check">✓</span> Scenario modelling</div>
            <div class="hc-item"><span class="hc-check">✓</span> Financial reporting</div>
            <div class="hc-item"><span class="hc-check">✓</span> Works with free Claude AI</div>
            <div class="hc-divider"></div>
            <div class="hc-price">$29</div>
            <div class="hc-price-sub">One-time · Interactive HTML</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── PROBLEM ── -->
  <section class="problem-section">
    <div class="container">
      <div class="problem-inner">
        <div class="gold-rule"></div>
        <p class="section-label">The Problem</p>
        <h2 class="section-h2">
          Most independent professionals<br /><em>know their numbers are</em><br />a problem.
        </h2>
        <p class="section-body">
          You're making decent money but have no idea what your actual margins are. Your pricing feels
          arbitrary. You don't have a forecast. You don't know if you're growing or just busy. You dread
          talking to accountants because you've never tracked your own data properly.
        </p>
        <p class="section-body">
          The problem isn't that you're bad with money. It's that you don't have a system for understanding it.
          This system turns cashflow chaos into clarity, pricing guesswork into strategy, and financial confusion
          into investor-grade reporting.
        </p>
        <blockquote class="sys-quote">
          "Most independent professionals know their numbers are a problem.
          They just don't have a system for fixing them.
          <strong>This system is that fix.</strong>"
        </blockquote>
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── ARCHITECTURE ── -->
  <section class="arch-section">
    <div class="container">
      <p class="section-label">System Architecture</p>
      <h2 class="section-h2">4 Modules. Financial Mastery.</h2>
      <div class="layers-grid">
        {layers.map(l => (
          <div class="layer-card">
            <div class="layer-n">Module {l.n}</div>
            <div class="layer-name">{l.name}</div>
            <div class="layer-desc">{l.desc}</div>
          </div>
        ))}
      </div>
      <div class="flow-panel">
        <div class="flow-label">Financial flow — from baseline tracking to strategic planning</div>
        <div class="flow-steps">
          <div class="flow-node flow-dashed">Your business today: revenue, expenses, margins unknown</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node">Module 1 — Financial Baseline (understand your current state)</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node">Module 2 — Pricing Architecture (value-based pricing strategy)</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node flow-gold">AI Synthesis — Cashflow forecasting + scenario analysis</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node flow-ink">Modules 3 & 4 — Strategic planning + investor-grade reporting</div>
        </div>
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── WHO IT'S FOR ── -->
  <section class="for-section">
    <div class="container">
      <p class="section-label">Who It's For</p>
      <h2 class="section-h2">Built for anyone<br /><em>who needs financial clarity.</em></h2>
      <div class="for-grid">
        {for_who.map(f => (
          <div class="for-card">
            <div class="for-role">{f.role}</div>
            <div class="for-need">{f.need}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── PROTOCOL ── -->
  <section class="protocol-section">
    <div class="container">
      <div class="protocol-inner">
        <p class="section-label">The Implementation Path</p>
        <h2 class="section-h2">Month by month.<br /><em>From chaos to clarity.</em></h2>
        <div class="timeline">
          <div class="t-item">
            <div class="t-time">Month 1</div>
            <div class="t-right">
              <div class="t-title">Run Module 01 — Financial Baseline</div>
              <div class="t-desc">Map all revenue and expenses. Understand your current P&L in 2 hours.</div>
            </div>
          </div>
          <div class="t-item">
            <div class="t-time">Month 2</div>
            <div class="t-right">
              <div class="t-title">Run Module 02 — Pricing Architecture</div>
              <div class="t-desc">Redesign your pricing strategy with value-based frameworks.</div>
            </div>
          </div>
          <div class="t-item">
            <div class="t-time">Month 3</div>
            <div class="t-right">
              <div class="t-title">Run Module 03 — Scenario Planning</div>
              <div class="t-desc">Model 3 growth scenarios. Know your cashflow runway and upside.</div>
            </div>
          </div>
          <div class="t-item">
            <div class="t-time">Ongoing (Monthly)</div>
            <div class="t-right">
              <div class="t-title">Run Module 04 — Reporting & Review</div>
              <div class="t-desc">Monthly financial review. Investor-ready reporting in 30 minutes.</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── CTA ── -->
  <CTABlock
    eyebrow="Stay Updated"
    headline="Coming Soon.<br /><em>$29 One-Time.</em>"
    subtext="Get notified when Financial Planning System launches. Complete financial clarity for founders and independent professionals. Cashflow, pricing, forecasting, and reporting."
    primaryLabel="Notify Me When Live"
    primaryHref="https://seedsystems.co/contact/"
    ghostLabel="Back to Systems"
    ghostHref="/"
  />

  <!-- ── NEXT SYSTEM ── -->
  <section class="next-section">
    <div class="container">
      <p class="section-label">Also in the Signature Series</p>
      <div class="next-grid">
        <a href="/systems/morning-intelligence/" class="next-card">
          <div class="next-id">SS-01</div>
          <div class="next-title">The Morning Intelligence System</div>
          <div class="next-desc">10 master prompts. 5 intelligence layers. 15 minutes every morning.</div>
        </a>
        <a href="/systems/operations-in-a-box/" class="next-card">
          <div class="next-id">SS-02</div>
          <div class="next-title">Operations in a Box</div>
          <div class="next-desc">Run your solo business like a firm.</div>
        </a>
        <a href="/systems/legal-documents/" class="next-card">
          <div class="next-id">SS-07</div>
          <div class="next-title">Legal Document System</div>
          <div class="next-desc">Protect yourself. Every engagement. Every time.</div>
        </a>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  .container { max-width: var(--container-sm); margin: 0 auto; padding: 0 48px; }
  .rule { border: none; border-top: 1px solid var(--rule); margin: 0; }
  .gold-rule { width: 48px; height: 2px; background: var(--gold); margin-bottom: 28px; }

  /* Breadcrumb */
  .breadcrumb {
    display: flex; align-items: center; gap: 10px;
    font-size: 11px; color: var(--ink-light); letter-spacing: 0.04em;
    margin-bottom: 48px; font-family: var(--sans);
  }
  .breadcrumb a { color: var(--ink-light); text-decoration: none; }
  .breadcrumb a:hover { color: var(--ink); }
  .sep { color: var(--rule); }

  /* HERO */
  .sys-hero { padding: 80px 0; background: var(--canvas); border-bottom: 1px solid var(--rule); }
  .hero-inner { display: grid; grid-template-columns: 1fr auto; gap: 64px; align-items: start; }
  .hero-tag {
    display: inline-block; font-size: 10px; font-weight: 600;
    letter-spacing: 0.16em; text-transform: uppercase; color: var(--gold-dark);
    margin-bottom: 20px; font-family: var(--sans);
  }
  .hero-h1 {
    font-family: var(--serif);
    font-size: clamp(36px, 5vw, 64px); font-weight: 300; line-height: 1.08;
    color: var(--ink); margin-bottom: 24px;
  }
  .hero-h1 em { font-style: italic; color: var(--gold-dark); }
  .hero-sub {
    font-size: 16px; font-weight: 300; color: var(--ink-mid);
    line-height: 1.75; max-width: 480px; margin-bottom: 40px;
  }
  .hero-actions { display: flex; gap: 14px; flex-wrap: wrap; }
  .btn-primary {
    background: var(--ink); color: var(--canvas);
    font-size: 11px; font-weight: 500; letter-spacing: 0.08em;
    text-transform: uppercase; padding: 14px 28px;
    text-decoration: none; display: inline-block;
    transition: opacity 0.2s; font-family: var(--sans);
  }
  .btn-primary:hover { opacity: 0.75; }
  .btn-ghost {
    background: transparent; border: 1px solid var(--rule);
    color: var(--ink-mid); font-size: 11px; font-weight: 400;
    letter-spacing: 0.06em; padding: 14px 28px;
    text-decoration: none; display: inline-block;
    transition: border-color 0.2s, color 0.2s; font-family: var(--sans);
  }
  .btn-ghost:hover { border-color: var(--ink); color: var(--ink); }

  /* Hero card */
  .hero-card {
    background: var(--canvas-2); border: 1px solid var(--rule);
    padding: 28px; min-width: 220px; display: flex; flex-direction: column; gap: 10px;
  }
  .hc-label {
    font-size: 10px; font-weight: 600; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); margin-bottom: 4px;
  }
  .hc-item { display: flex; gap: 10px; font-size: 13px; font-weight: 300; color: var(--ink-mid); }
  .hc-check { color: var(--gold-dark); font-weight: 600; flex-shrink: 0; font-family: var(--sans); }
  .hc-divider { border-top: 1px solid var(--rule); margin: 8px 0; }
  .hc-price {
    font-family: var(--serif); font-size: 36px; font-weight: 300; color: var(--ink); line-height: 1;
  }
  .hc-price-sub { font-size: 11px; font-weight: 300; color: var(--ink-light); font-family: var(--sans); }

  /* SECTIONS */
  .section-label {
    display: block; font-size: 10px; font-weight: 600; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); margin-bottom: 16px;
  }
  .section-h2 {
    font-family: var(--serif);
    font-size: clamp(28px, 3.5vw, 44px); font-weight: 300; line-height: 1.12;
    color: var(--ink); margin-bottom: 24px;
  }
  .section-h2 em { font-style: italic; color: var(--gold-dark); }
  .section-body {
    font-size: 16px; font-weight: 300; color: var(--ink-mid);
    line-height: 1.8; margin-bottom: 20px; max-width: 640px;
  }

  /* PROBLEM */
  .problem-section { padding: 100px 0; background: var(--canvas-2); border-bottom: 1px solid var(--rule); }
  .problem-inner { max-width: 700px; }
  .sys-quote {
    font-family: var(--serif);
    font-size: 20px; font-weight: 300; font-style: italic;
    line-height: 1.65; color: var(--ink-mid);
    border-left: 2px solid var(--gold); padding-left: 28px;
    margin-top: 36px;
  }
  .sys-quote strong { font-style: normal; color: var(--ink); font-weight: 400; }

  /* ARCHITECTURE */
  .arch-section { padding: 100px 0; background: var(--canvas); border-bottom: 1px solid var(--rule); }
  .layers-grid {
    display: grid; grid-template-columns: repeat(2, 1fr);
    gap: 1px; background: var(--rule); border: 1px solid var(--rule);
    margin: 40px 0;
  }
  .layer-card { background: var(--white); padding: 28px; }
  .layer-card:hover { background: var(--canvas-2); }
  .layer-output { background: var(--canvas-2); }
  .layer-n {
    font-size: 10px; font-weight: 600; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); margin-bottom: 8px;
  }
  .layer-name {
    font-family: var(--serif); font-size: 18px; font-weight: 300;
    color: var(--ink); margin-bottom: 8px;
  }
  .layer-desc { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.55; }

  .flow-panel {
    background: var(--canvas-2); border: 1px solid var(--rule); padding: 32px;
  }
  .flow-label {
    font-size: 10px; font-weight: 600; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--ink-light);
    font-family: var(--sans); margin-bottom: 28px;
  }
  .flow-steps { display: flex; flex-direction: column; align-items: center; gap: 0; }
  .flow-node {
    border: 1px solid var(--rule); background: var(--white);
    color: var(--ink-mid); font-size: 13px; font-weight: 300;
    padding: 12px 24px; text-align: center; width: 100%; max-width: 520px;
    font-family: var(--sans);
  }
  .flow-dashed { border-style: dashed; color: var(--ink-light); background: transparent; }
  .flow-gold { background: var(--gold); border-color: var(--gold); color: var(--ink); font-weight: 400; }
  .flow-ink { background: var(--ink); border-color: var(--ink); color: var(--canvas); font-weight: 400; }
  .flow-arrow { font-size: 18px; color: var(--gold-dark); padding: 6px 0; line-height: 1; }

  /* FOR WHO */
  .for-section { padding: 100px 0; background: var(--canvas-2); border-bottom: 1px solid var(--rule); }
  .for-grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px;
    background: var(--rule); border: 1px solid var(--rule); margin-top: 40px;
  }
  .for-card { background: var(--white); padding: 28px; }
  .for-role {
    font-family: var(--serif); font-size: 18px; font-weight: 300;
    color: var(--ink); margin-bottom: 12px;
  }
  .for-need { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.6; }

  /* PROTOCOL */
  .protocol-section { padding: 100px 0; background: var(--canvas); border-bottom: 1px solid var(--rule); }
  .protocol-inner { max-width: 680px; }
  .timeline { margin-top: 40px; border-left: 1px solid var(--rule); padding-left: 32px; }
  .t-item {
    display: grid; grid-template-columns: 100px 1fr;
    gap: 24px; padding: 0 0 32px;
  }
  .t-time {
    font-size: 10px; font-weight: 600; letter-spacing: 0.1em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); padding-top: 3px; line-height: 1.4;
  }
  .t-title { font-size: 15px; font-weight: 400; color: var(--ink); margin-bottom: 6px; }
  .t-desc { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.6; }

  /* NEXT */
  .next-section { padding: 80px 0; background: var(--canvas-2); border-top: 1px solid var(--rule); }
  .next-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--rule); border: 1px solid var(--rule); margin-top: 32px; }
  .next-card {
    background: var(--white); padding: 28px;
    text-decoration: none; color: inherit; transition: background 0.2s;
  }
  .next-card:hover { background: var(--canvas-2); }
  .next-id { font-size: 10px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--gold-dark); font-family: var(--sans); margin-bottom: 10px; }
  .next-title { font-family: var(--serif); font-size: 17px; font-weight: 300; color: var(--ink); margin-bottom: 8px; }
  .next-desc { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.55; }

  @media (max-width: 900px) {
    .hero-inner { grid-template-columns: 1fr; }
    .hero-right { display: none; }
    .layers-grid { grid-template-columns: 1fr; }
    .for-grid { grid-template-columns: repeat(2, 1fr); }
    .next-grid { grid-template-columns: 1fr; }
    .t-item { grid-template-columns: 80px 1fr; }
  }
  @media (max-width: 640px) {
    .container { padding: 0 24px; }
    .layers-grid { grid-template-columns: 1fr; }
    .for-grid { grid-template-columns: 1fr; }
  }
</style>
