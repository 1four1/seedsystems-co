---
import PageLayout from '../../../layouts/PageLayout.astro';
import CTABlock from '../../../components/CTABlock.astro';

const layers = [
  { n: '01', name: 'Document Foundation', desc: 'Contract templates, clause libraries, and proposal-to-contract conversion frameworks.' },
  { n: '02', name: 'Engagement Protection', desc: 'Scope management, change request handling, and dispute prevention prompts.' },
  { n: '03', name: 'Legal Communication', desc: 'Client communication scripts for difficult situations, non-payment recovery, and professional close-out.' },
];

const for_who = [
  { role: 'Freelancers', need: 'Who use informal agreements and get burned when projects go wrong.' },
  { role: 'Consultants', need: 'Who need enforceable contracts without expensive solicitor fees.' },
  { role: 'Agency Owners', need: 'Who need consistent legal documentation across multiple client relationships.' },
  { role: 'Independent Contractors', need: 'Who want to look and operate like a properly governed firm.' },
  { role: 'Small Business Owners', need: 'Who need legal communication frameworks for supplier and client disputes.' },
  { role: 'Any Independent Professional', need: 'Who wants professional legal infrastructure at a non-legal price.' },
];
---

<PageLayout
  title="Legal Document System — SeedSystems™ Signature Systems"
  description="AI-powered contract, compliance, and legal communication system. 3 layers. Protect yourself. Every engagement. Every time. Coming soon."
  activePath="/systems/"
>
  <!-- ── HERO ── -->
  <section class="sys-hero">
    <div class="container">
      <div class="breadcrumb">
        <a href="/">SeedSystems™</a>
        <span class="sep">→</span>
        <a href="/systems/">Signature Systems</a>
        <span class="sep">→</span>
        <span>Legal Documents</span>
      </div>
      <div class="hero-inner">
        <div class="hero-left">
          <span class="hero-tag">SS-07 · Legal Series · In Build</span>
          <h1 class="hero-h1">
            Legal Document<br /><em>System</em>
          </h1>
          <p class="hero-sub">
            Protect yourself. Every engagement. Every time. AI-powered contract, compliance,
            and legal communication system for independent professionals and small businesses.
          </p>
          <div class="hero-actions">
            <a href="https://seedsystems.co/contact/" class="btn-primary" target="_blank" rel="noopener">
              Notify Me When Live
            </a>
            <a href="/" class="btn-ghost" target="_blank" rel="noopener">
              Back to Systems
            </a>
          </div>
        </div>
        <div class="hero-right">
          <div class="hero-card">
            <div class="hc-label">Coming Soon</div>
            <div class="hc-item"><span class="hc-check">✓</span> 3 legal layers</div>
            <div class="hc-item"><span class="hc-check">✓</span> 15+ master prompts</div>
            <div class="hc-item"><span class="hc-check">✓</span> Contract templates</div>
            <div class="hc-item"><span class="hc-check">✓</span> Clause libraries</div>
            <div class="hc-item"><span class="hc-check">✓</span> Dispute prevention</div>
            <div class="hc-item"><span class="hc-check">✓</span> Legal communication scripts</div>
            <div class="hc-item"><span class="hc-check">✓</span> Works with free Claude AI</div>
            <div class="hc-divider"></div>
            <div class="hc-price">$29</div>
            <div class="hc-price-sub">One-time · Interactive HTML</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── PROBLEM ── -->
  <section class="problem-section">
    <div class="container">
      <div class="problem-inner">
        <div class="gold-rule"></div>
        <p class="section-label">The Problem</p>
        <h2 class="section-h2">
          Most independent professionals<br /><em>don't have bad clients.</em><br />They have bad contracts.
        </h2>
        <p class="section-body">
          You have a handshake agreement or a casual email confirmation. The project scope isn't defined.
          There's no change order process. You're not paid until you've delivered everything. When things go wrong,
          you have nothing to fall back on.
        </p>
        <p class="section-body">
          The problem isn't the clients. It's the legal infrastructure. You don't need expensive lawyers.
          You need professional contract templates, scope management frameworks, and communication scripts that
          prevent disputes before they happen.
        </p>
        <blockquote class="sys-quote">
          "Most independent professionals don't have bad clients. They have bad contracts.
          <strong>This system fixes that.</strong>"
        </blockquote>
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── ARCHITECTURE ── -->
  <section class="arch-section">
    <div class="container">
      <p class="section-label">System Architecture</p>
      <h2 class="section-h2">3 Layers. Legal Mastery.</h2>
      <div class="layers-grid">
        {layers.map(l => (
          <div class="layer-card">
            <div class="layer-n">Layer {l.n}</div>
            <div class="layer-name">{l.name}</div>
            <div class="layer-desc">{l.desc}</div>
          </div>
        ))}
      </div>
      <div class="flow-panel">
        <div class="flow-label">Legal flow — from foundation to engagement to protection</div>
        <div class="flow-steps">
          <div class="flow-node flow-dashed">New client engagement begins</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node">Layer 1 — Document Foundation (professional contract from templates)</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node">Layer 2 — Engagement Protection (scope management + change orders)</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node flow-gold">AI Synthesis — Dispute prevention + professional communication</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node flow-ink">Layer 3 — Legal Communication (crisis management + recovery)</div>
        </div>
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── WHO IT'S FOR ── -->
  <section class="for-section">
    <div class="container">
      <p class="section-label">Who It's For</p>
      <h2 class="section-h2">Built for anyone<br /><em>who needs legal infrastructure.</em></h2>
      <div class="for-grid">
        {for_who.map(f => (
          <div class="for-card">
            <div class="for-role">{f.role}</div>
            <div class="for-need">{f.need}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── PROTOCOL ── -->
  <section class="protocol-section">
    <div class="container">
      <div class="protocol-inner">
        <p class="section-label">The Implementation Path</p>
        <h2 class="section-h2">From handshake<br /><em>to professional contract.</em></h2>
        <div class="timeline">
          <div class="t-item">
            <div class="t-time">New Client</div>
            <div class="t-right">
              <div class="t-title">Layer 01 — Generate Your Contract</div>
              <div class="t-desc">Run the contract generator with project details. Professional agreement in 20 minutes.</div>
            </div>
          </div>
          <div class="t-item">
            <div class="t-time">Project In Progress</div>
            <div class="t-right">
              <div class="t-title">Layer 02 — Scope Management</div>
              <div class="t-desc">Use change request templates. Prevent scope creep before it happens.</div>
            </div>
          </div>
          <div class="t-item">
            <div class="t-time">If Issues Arise</div>
            <div class="t-right">
              <div class="t-title">Layer 03 — Legal Communication Scripts</div>
              <div class="t-desc">Non-payment recovery, scope disputes, difficult conversations. Scripts ready to use.</div>
            </div>
          </div>
          <div class="t-item">
            <div class="t-time">Every Engagement</div>
            <div class="t-right">
              <div class="t-title">Professional Close-Out</div>
              <div class="t-desc">Professional close-out and dispute prevention frameworks for every engagement.</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── CTA ── -->
  <CTABlock
    eyebrow="Stay Updated"
    headline="Coming Soon.<br /><em>$29 One-Time.</em>"
    subtext="Get notified when Legal Document System launches. Professional legal infrastructure for freelancers and small businesses. Contracts. Scope management. Communication scripts."
    primaryLabel="Notify Me When Live"
    primaryHref="https://seedsystems.co/contact/"
    ghostLabel="Back to Systems"
    ghostHref="/"
  />

  <!-- ── NEXT SYSTEM ── -->
  <section class="next-section">
    <div class="container">
      <p class="section-label">Also in the Signature Series</p>
      <div class="next-grid">
        <a href="/systems/operations-in-a-box/" class="next-card">
          <div class="next-id">SS-02</div>
          <div class="next-title">Operations in a Box</div>
          <div class="next-desc">Run your solo business like a firm.</div>
        </a>
        <a href="/systems/financial-planning/" class="next-card">
          <div class="next-id">SS-06</div>
          <div class="next-title">Financial Planning System</div>
          <div class="next-desc">Know your numbers. Own your decisions.</div>
        </a>
        <a href="/systems/freelancer-command-center/" class="next-card">
          <div class="next-id">SS-09</div>
          <div class="next-title">Freelancer Command Center</div>
          <div class="next-desc">Stop running your business from your inbox.</div>
        </a>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  .container { max-width: var(--container-sm); margin: 0 auto; padding: 0 48px; }
  .rule { border: none; border-top: 1px solid var(--rule); margin: 0; }
  .gold-rule { width: 48px; height: 2px; background: var(--gold); margin-bottom: 28px; }

  /* Breadcrumb */
  .breadcrumb {
    display: flex; align-items: center; gap: 10px;
    font-size: 11px; color: var(--ink-light); letter-spacing: 0.04em;
    margin-bottom: 48px; font-family: var(--sans);
  }
  .breadcrumb a { color: var(--ink-light); text-decoration: none; }
  .breadcrumb a:hover { color: var(--ink); }
  .sep { color: var(--rule); }

  /* HERO */
  .sys-hero { padding: 80px 0; background: var(--canvas); border-bottom: 1px solid var(--rule); }
  .hero-inner { display: grid; grid-template-columns: 1fr auto; gap: 64px; align-items: start; }
  .hero-tag {
    display: inline-block; font-size: 10px; font-weight: 600;
    letter-spacing: 0.16em; text-transform: uppercase; color: var(--gold-dark);
    margin-bottom: 20px; font-family: var(--sans);
  }
  .hero-h1 {
    font-family: var(--serif);
    font-size: clamp(36px, 5vw, 64px); font-weight: 300; line-height: 1.08;
    color: var(--ink); margin-bottom: 24px;
  }
  .hero-h1 em { font-style: italic; color: var(--gold-dark); }
  .hero-sub {
    font-size: 16px; font-weight: 300; color: var(--ink-mid);
    line-height: 1.75; max-width: 480px; margin-bottom: 40px;
  }
  .hero-actions { display: flex; gap: 14px; flex-wrap: wrap; }
  .btn-primary {
    background: var(--ink); color: var(--canvas);
    font-size: 11px; font-weight: 500; letter-spacing: 0.08em;
    text-transform: uppercase; padding: 14px 28px;
    text-decoration: none; display: inline-block;
    transition: opacity 0.2s; font-family: var(--sans);
  }
  .btn-primary:hover { opacity: 0.75; }
  .btn-ghost {
    background: transparent; border: 1px solid var(--rule);
    color: var(--ink-mid); font-size: 11px; font-weight: 400;
    letter-spacing: 0.06em; padding: 14px 28px;
    text-decoration: none; display: inline-block;
    transition: border-color 0.2s, color 0.2s; font-family: var(--sans);
  }
  .btn-ghost:hover { border-color: var(--ink); color: var(--ink); }

  /* Hero card */
  .hero-card {
    background: var(--canvas-2); border: 1px solid var(--rule);
    padding: 28px; min-width: 220px; display: flex; flex-direction: column; gap: 10px;
  }
  .hc-label {
    font-size: 10px; font-weight: 600; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); margin-bottom: 4px;
  }
  .hc-item { display: flex; gap: 10px; font-size: 13px; font-weight: 300; color: var(--ink-mid); }
  .hc-check { color: var(--gold-dark); font-weight: 600; flex-shrink: 0; font-family: var(--sans); }
  .hc-divider { border-top: 1px solid var(--rule); margin: 8px 0; }
  .hc-price {
    font-family: var(--serif); font-size: 36px; font-weight: 300; color: var(--ink); line-height: 1;
  }
  .hc-price-sub { font-size: 11px; font-weight: 300; color: var(--ink-light); font-family: var(--sans); }

  /* SECTIONS */
  .section-label {
    display: block; font-size: 10px; font-weight: 600; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); margin-bottom: 16px;
  }
  .section-h2 {
    font-family: var(--serif);
    font-size: clamp(28px, 3.5vw, 44px); font-weight: 300; line-height: 1.12;
    color: var(--ink); margin-bottom: 24px;
  }
  .section-h2 em { font-style: italic; color: var(--gold-dark); }
  .section-body {
    font-size: 16px; font-weight: 300; color: var(--ink-mid);
    line-height: 1.8; margin-bottom: 20px; max-width: 640px;
  }

  /* PROBLEM */
  .problem-section { padding: 100px 0; background: var(--canvas-2); border-bottom: 1px solid var(--rule); }
  .problem-inner { max-width: 700px; }
  .sys-quote {
    font-family: var(--serif);
    font-size: 20px; font-weight: 300; font-style: italic;
    line-height: 1.65; color: var(--ink-mid);
    border-left: 2px solid var(--gold); padding-left: 28px;
    margin-top: 36px;
  }
  .sys-quote strong { font-style: normal; color: var(--ink); font-weight: 400; }

  /* ARCHITECTURE */
  .arch-section { padding: 100px 0; background: var(--canvas); border-bottom: 1px solid var(--rule); }
  .layers-grid {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 1px; background: var(--rule); border: 1px solid var(--rule);
    margin: 40px 0;
  }
  .layer-card { background: var(--white); padding: 28px; }
  .layer-card:hover { background: var(--canvas-2); }
  .layer-output { background: var(--canvas-2); }
  .layer-n {
    font-size: 10px; font-weight: 600; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); margin-bottom: 8px;
  }
  .layer-name {
    font-family: var(--serif); font-size: 18px; font-weight: 300;
    color: var(--ink); margin-bottom: 8px;
  }
  .layer-desc { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.55; }

  .flow-panel {
    background: var(--canvas-2); border: 1px solid var(--rule); padding: 32px;
  }
  .flow-label {
    font-size: 10px; font-weight: 600; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--ink-light);
    font-family: var(--sans); margin-bottom: 28px;
  }
  .flow-steps { display: flex; flex-direction: column; align-items: center; gap: 0; }
  .flow-node {
    border: 1px solid var(--rule); background: var(--white);
    color: var(--ink-mid); font-size: 13px; font-weight: 300;
    padding: 12px 24px; text-align: center; width: 100%; max-width: 520px;
    font-family: var(--sans);
  }
  .flow-dashed { border-style: dashed; color: var(--ink-light); background: transparent; }
  .flow-gold { background: var(--gold); border-color: var(--gold); color: var(--ink); font-weight: 400; }
  .flow-ink { background: var(--ink); border-color: var(--ink); color: var(--canvas); font-weight: 400; }
  .flow-arrow { font-size: 18px; color: var(--gold-dark); padding: 6px 0; line-height: 1; }

  /* FOR WHO */
  .for-section { padding: 100px 0; background: var(--canvas-2); border-bottom: 1px solid var(--rule); }
  .for-grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px;
    background: var(--rule); border: 1px solid var(--rule); margin-top: 40px;
  }
  .for-card { background: var(--white); padding: 28px; }
  .for-role {
    font-family: var(--serif); font-size: 18px; font-weight: 300;
    color: var(--ink); margin-bottom: 12px;
  }
  .for-need { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.6; }

  /* PROTOCOL */
  .protocol-section { padding: 100px 0; background: var(--canvas); border-bottom: 1px solid var(--rule); }
  .protocol-inner { max-width: 680px; }
  .timeline { margin-top: 40px; border-left: 1px solid var(--rule); padding-left: 32px; }
  .t-item {
    display: grid; grid-template-columns: 100px 1fr;
    gap: 24px; padding: 0 0 32px;
  }
  .t-time {
    font-size: 10px; font-weight: 600; letter-spacing: 0.1em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); padding-top: 3px; line-height: 1.4;
  }
  .t-title { font-size: 15px; font-weight: 400; color: var(--ink); margin-bottom: 6px; }
  .t-desc { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.6; }

  /* NEXT */
  .next-section { padding: 80px 0; background: var(--canvas-2); border-top: 1px solid var(--rule); }
  .next-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--rule); border: 1px solid var(--rule); margin-top: 32px; }
  .next-card {
    background: var(--white); padding: 28px;
    text-decoration: none; color: inherit; transition: background 0.2s;
  }
  .next-card:hover { background: var(--canvas-2); }
  .next-id { font-size: 10px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--gold-dark); font-family: var(--sans); margin-bottom: 10px; }
  .next-title { font-family: var(--serif); font-size: 17px; font-weight: 300; color: var(--ink); margin-bottom: 8px; }
  .next-desc { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.55; }

  @media (max-width: 900px) {
    .hero-inner { grid-template-columns: 1fr; }
    .hero-right { display: none; }
    .layers-grid { grid-template-columns: repeat(2, 1fr); }
    .for-grid { grid-template-columns: repeat(2, 1fr); }
    .next-grid { grid-template-columns: 1fr; }
    .t-item { grid-template-columns: 80px 1fr; }
  }
  @media (max-width: 640px) {
    .container { padding: 0 24px; }
    .layers-grid { grid-template-columns: 1fr; }
    .for-grid { grid-template-columns: 1fr; }
  }
</style>
