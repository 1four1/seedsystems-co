---
import PageLayout from '../../../layouts/PageLayout.astro';
import CTABlock from '../../../components/CTABlock.astro';

const layers = [
  { n: '01', name: 'Intelligence (Pre-Sales)', desc: 'ICP definition, account research, trigger mapping, and competitive positioning before a single outreach.' },
  { n: '02', name: 'Execution (Active Sales)', desc: 'Outreach, discovery calls, objection handling, proposal writing, and follow-up sequences.' },
  { n: '03', name: 'Pipeline Management', desc: 'Deal tracking, pipeline reviews, conversion analysis, and forecasting.' },
];

const for_who = [
  { role: 'Founders', need: 'Who are their own sales team and need to close without a sales background.' },
  { role: 'Freelancers', need: 'Who dread business development but need consistent pipeline.' },
  { role: 'Account Executives', need: 'Who want to go from quota-meeting to quota-crushing.' },
  { role: 'B2B Consultants', need: 'Who sell expertise and need to communicate value without discounting.' },
  { role: 'Business Development Leads', need: 'Managing complex, multi-stakeholder deals.' },
  { role: 'Anyone Who Sells', need: 'Who replaces improvisation with intelligence.' },
];
---

<PageLayout
  title="Sales Intelligence System — SeedSystems™ Signature Systems"
  description="Full-cycle AI sales system from ICP to close. 3 layers. Built for founders and sales teams who need quota-crushing. $29 one-time."
  activePath="/systems/"
>
  <!-- ── HERO ── -->
  <section class="sys-hero">
    <div class="container">
      <div class="breadcrumb">
        <a href="/">SeedSystems™</a>
        <span class="sep">→</span>
        <a href="/systems/">Signature Systems</a>
        <span class="sep">→</span>
        <span>Sales Intelligence</span>
      </div>
      <div class="hero-inner">
        <div class="hero-left">
          <span class="hero-tag">SS-05 · Sales Series</span>
          <h1 class="hero-h1">
            Sales<br /><em>Intelligence System</em>
          </h1>
          <p class="hero-sub">
            Stop chasing leads. Start engineering pipeline. Full-cycle AI sales system
            from ICP definition to close. Built for founders and account executives.
          </p>
          <div class="hero-actions">
            <a href="https://seedsystems.gumroad.com/l/sales-intelligence" class="btn-primary" target="_blank" rel="noopener">
              Get this system — $29
            </a>
            <a href="https://seedsystems.gumroad.com/l/sales-intelligence-free" class="btn-ghost" target="_blank" rel="noopener">
              Try one prompt free
            </a>
          </div>
        </div>
        <div class="hero-right">
          <div class="hero-card">
            <div class="hc-label">What you get</div>
            <div class="hc-item"><span class="hc-check">✓</span> 3 sales layers</div>
            <div class="hc-item"><span class="hc-check">✓</span> 20+ master prompts</div>
            <div class="hc-item"><span class="hc-check">✓</span> ICP definition framework</div>
            <div class="hc-item"><span class="hc-check">✓</span> Account research templates</div>
            <div class="hc-item"><span class="hc-check">✓</span> Objection handling scripts</div>
            <div class="hc-item"><span class="hc-check">✓</span> Pipeline forecasting</div>
            <div class="hc-item"><span class="hc-check">✓</span> Works with free Claude AI</div>
            <div class="hc-divider"></div>
            <div class="hc-price">$29</div>
            <div class="hc-price-sub">One-time · Interactive HTML</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── PROBLEM ── -->
  <section class="problem-section">
    <div class="container">
      <div class="problem-inner">
        <div class="gold-rule"></div>
        <p class="section-label">The Problem</p>
        <h2 class="section-h2">
          Most salespeople chase<br /><em>leads instead of engineering</em><br />pipeline.
        </h2>
        <p class="section-body">
          You're outreaching to cold accounts without research. You're going into discovery calls
          unprepared. You're handling objections reactively. You're writing proposals from scratch.
          You're following up on gut instinct instead of data. And your pipeline is a mystery.
        </p>
        <p class="section-body">
          The top salespeople don't have better leads or warmer markets. They have better information
          and a repeatable system. They know their ICP. They research before outreach. They have
          scripts. They track pipeline. This system builds all of that.
        </p>
        <blockquote class="sys-quote">
          "Stop chasing leads. Start engineering pipeline. The top salespeople don't have better
          products or warmer leads. They have better information and a repeatable system.
          <strong>This system is that.</strong>"
        </blockquote>
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── ARCHITECTURE ── -->
  <section class="arch-section">
    <div class="container">
      <p class="section-label">System Architecture</p>
      <h2 class="section-h2">3 Layers. From ICP to Close.</h2>
      <div class="layers-grid">
        {layers.map(l => (
          <div class="layer-card">
            <div class="layer-n">Layer {l.n}</div>
            <div class="layer-name">{l.name}</div>
            <div class="layer-desc">{l.desc}</div>
          </div>
        ))}
      </div>
      <div class="flow-panel">
        <div class="flow-label">Sales flow — from ICP to customer</div>
        <div class="flow-steps">
          <div class="flow-node flow-dashed">Prospect or account identified</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node">Layer 1 — Intelligence (ICP match + research + positioning)</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node">Layer 2 — Execution (Outreach + discovery + objection handling + proposals)</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node flow-gold">AI Synthesis — Deal health monitoring + pipeline forecasting</div>
          <div class="flow-arrow">↓</div>
          <div class="flow-node flow-ink">Layer 3 — Pipeline Management (Deal tracking + conversion analysis)</div>
        </div>
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── WHO IT'S FOR ── -->
  <section class="for-section">
    <div class="container">
      <p class="section-label">Who It's For</p>
      <h2 class="section-h2">Built for anyone<br /><em>who sells for a living.</em></h2>
      <div class="for-grid">
        {for_who.map(f => (
          <div class="for-card">
            <div class="for-role">{f.role}</div>
            <div class="for-need">{f.need}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <hr class="rule" />

  <!-- ── PROTOCOL ── -->
  <section class="protocol-section">
    <div class="container">
      <div class="protocol-inner">
        <p class="section-label">The Sales Protocol</p>
        <h2 class="section-h2">Research. Execute.<br /><em>Track. Repeat.</em></h2>
        <div class="timeline">
          <div class="t-item">
            <div class="t-time">Before Outreach (15 min)</div>
            <div class="t-right">
              <div class="t-title">Run Layer 01 — Account Research</div>
              <div class="t-desc">ICP match check, account research, trigger mapping, competitive positioning.</div>
            </div>
          </div>
          <div class="t-item">
            <div class="t-time">Before Discovery Calls (5 min)</div>
            <div class="t-right">
              <div class="t-title">Run Discovery Prep Prompt</div>
              <div class="t-desc">Meeting intelligence and discovery framework in 5 minutes.</div>
            </div>
          </div>
          <div class="t-item">
            <div class="t-time">After Proposals (2 min)</div>
            <div class="t-right">
              <div class="t-title">Run Follow-up Sequence Prompt</div>
              <div class="t-desc">3-touch follow-up sequence generated and ready to execute.</div>
            </div>
          </div>
          <div class="t-item">
            <div class="t-time">Weekly</div>
            <div class="t-right">
              <div class="t-title">Run Pipeline Review Prompt</div>
              <div class="t-desc">Deal health, priority ranking, conversion analysis, and forecasting.</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── CTA ── -->
  <CTABlock
    eyebrow="Get started"
    headline="$29. Engineering<br /><em>your sales.</em>"
    subtext="Interactive HTML. Complete full-cycle sales system. From ICP definition through discovery, objection handling, proposals, and pipeline management. Or try one prompt free."
    primaryLabel="Get the full system — $29"
    primaryHref="https://seedsystems.gumroad.com/l/sales-intelligence"
    ghostLabel="Try one prompt free"
    ghostHref="https://seedsystems.gumroad.com/l/sales-intelligence-free"
  />

  <!-- ── NEXT SYSTEM ── -->
  <section class="next-section">
    <div class="container">
      <p class="section-label">Also in the Signature Series</p>
      <div class="next-grid">
        <a href="/systems/morning-intelligence/" class="next-card">
          <div class="next-id">SS-01</div>
          <div class="next-title">The Morning Intelligence System</div>
          <div class="next-desc">10 master prompts. 5 intelligence layers. 15 minutes every morning.</div>
        </a>
        <a href="/systems/operations-in-a-box/" class="next-card">
          <div class="next-id">SS-02</div>
          <div class="next-title">Operations in a Box</div>
          <div class="next-desc">Run your solo business like a firm.</div>
        </a>
        <a href="/systems/ceo-intelligence/" class="next-card">
          <div class="next-id">SS-08</div>
          <div class="next-title">The CEO Intelligence Briefing</div>
          <div class="next-desc">Executive-grade intelligence across five CEO functions.</div>
        </a>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  .container { max-width: var(--container-sm); margin: 0 auto; padding: 0 48px; }
  .rule { border: none; border-top: 1px solid var(--rule); margin: 0; }
  .gold-rule { width: 48px; height: 2px; background: var(--gold); margin-bottom: 28px; }

  /* Breadcrumb */
  .breadcrumb {
    display: flex; align-items: center; gap: 10px;
    font-size: 11px; color: var(--ink-light); letter-spacing: 0.04em;
    margin-bottom: 48px; font-family: var(--sans);
  }
  .breadcrumb a { color: var(--ink-light); text-decoration: none; }
  .breadcrumb a:hover { color: var(--ink); }
  .sep { color: var(--rule); }

  /* HERO */
  .sys-hero { padding: 80px 0; background: var(--canvas); border-bottom: 1px solid var(--rule); }
  .hero-inner { display: grid; grid-template-columns: 1fr auto; gap: 64px; align-items: start; }
  .hero-tag {
    display: inline-block; font-size: 10px; font-weight: 600;
    letter-spacing: 0.16em; text-transform: uppercase; color: var(--gold-dark);
    margin-bottom: 20px; font-family: var(--sans);
  }
  .hero-h1 {
    font-family: var(--serif);
    font-size: clamp(36px, 5vw, 64px); font-weight: 300; line-height: 1.08;
    color: var(--ink); margin-bottom: 24px;
  }
  .hero-h1 em { font-style: italic; color: var(--gold-dark); }
  .hero-sub {
    font-size: 16px; font-weight: 300; color: var(--ink-mid);
    line-height: 1.75; max-width: 480px; margin-bottom: 40px;
  }
  .hero-actions { display: flex; gap: 14px; flex-wrap: wrap; }
  .btn-primary {
    background: var(--ink); color: var(--canvas);
    font-size: 11px; font-weight: 500; letter-spacing: 0.08em;
    text-transform: uppercase; padding: 14px 28px;
    text-decoration: none; display: inline-block;
    transition: opacity 0.2s; font-family: var(--sans);
  }
  .btn-primary:hover { opacity: 0.75; }
  .btn-ghost {
    background: transparent; border: 1px solid var(--rule);
    color: var(--ink-mid); font-size: 11px; font-weight: 400;
    letter-spacing: 0.06em; padding: 14px 28px;
    text-decoration: none; display: inline-block;
    transition: border-color 0.2s, color 0.2s; font-family: var(--sans);
  }
  .btn-ghost:hover { border-color: var(--ink); color: var(--ink); }

  /* Hero card */
  .hero-card {
    background: var(--canvas-2); border: 1px solid var(--rule);
    padding: 28px; min-width: 220px; display: flex; flex-direction: column; gap: 10px;
  }
  .hc-label {
    font-size: 10px; font-weight: 600; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); margin-bottom: 4px;
  }
  .hc-item { display: flex; gap: 10px; font-size: 13px; font-weight: 300; color: var(--ink-mid); }
  .hc-check { color: var(--gold-dark); font-weight: 600; flex-shrink: 0; font-family: var(--sans); }
  .hc-divider { border-top: 1px solid var(--rule); margin: 8px 0; }
  .hc-price {
    font-family: var(--serif); font-size: 36px; font-weight: 300; color: var(--ink); line-height: 1;
  }
  .hc-price-sub { font-size: 11px; font-weight: 300; color: var(--ink-light); font-family: var(--sans); }

  /* SECTIONS */
  .section-label {
    display: block; font-size: 10px; font-weight: 600; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); margin-bottom: 16px;
  }
  .section-h2 {
    font-family: var(--serif);
    font-size: clamp(28px, 3.5vw, 44px); font-weight: 300; line-height: 1.12;
    color: var(--ink); margin-bottom: 24px;
  }
  .section-h2 em { font-style: italic; color: var(--gold-dark); }
  .section-body {
    font-size: 16px; font-weight: 300; color: var(--ink-mid);
    line-height: 1.8; margin-bottom: 20px; max-width: 640px;
  }

  /* PROBLEM */
  .problem-section { padding: 100px 0; background: var(--canvas-2); border-bottom: 1px solid var(--rule); }
  .problem-inner { max-width: 700px; }
  .sys-quote {
    font-family: var(--serif);
    font-size: 20px; font-weight: 300; font-style: italic;
    line-height: 1.65; color: var(--ink-mid);
    border-left: 2px solid var(--gold); padding-left: 28px;
    margin-top: 36px;
  }
  .sys-quote strong { font-style: normal; color: var(--ink); font-weight: 400; }

  /* ARCHITECTURE */
  .arch-section { padding: 100px 0; background: var(--canvas); border-bottom: 1px solid var(--rule); }
  .layers-grid {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 1px; background: var(--rule); border: 1px solid var(--rule);
    margin: 40px 0;
  }
  .layer-card { background: var(--white); padding: 28px; }
  .layer-card:hover { background: var(--canvas-2); }
  .layer-output { background: var(--canvas-2); }
  .layer-n {
    font-size: 10px; font-weight: 600; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); margin-bottom: 8px;
  }
  .layer-name {
    font-family: var(--serif); font-size: 18px; font-weight: 300;
    color: var(--ink); margin-bottom: 8px;
  }
  .layer-desc { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.55; }

  .flow-panel {
    background: var(--canvas-2); border: 1px solid var(--rule); padding: 32px;
  }
  .flow-label {
    font-size: 10px; font-weight: 600; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--ink-light);
    font-family: var(--sans); margin-bottom: 28px;
  }
  .flow-steps { display: flex; flex-direction: column; align-items: center; gap: 0; }
  .flow-node {
    border: 1px solid var(--rule); background: var(--white);
    color: var(--ink-mid); font-size: 13px; font-weight: 300;
    padding: 12px 24px; text-align: center; width: 100%; max-width: 520px;
    font-family: var(--sans);
  }
  .flow-dashed { border-style: dashed; color: var(--ink-light); background: transparent; }
  .flow-gold { background: var(--gold); border-color: var(--gold); color: var(--ink); font-weight: 400; }
  .flow-ink { background: var(--ink); border-color: var(--ink); color: var(--canvas); font-weight: 400; }
  .flow-arrow { font-size: 18px; color: var(--gold-dark); padding: 6px 0; line-height: 1; }

  /* FOR WHO */
  .for-section { padding: 100px 0; background: var(--canvas-2); border-bottom: 1px solid var(--rule); }
  .for-grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px;
    background: var(--rule); border: 1px solid var(--rule); margin-top: 40px;
  }
  .for-card { background: var(--white); padding: 28px; }
  .for-role {
    font-family: var(--serif); font-size: 18px; font-weight: 300;
    color: var(--ink); margin-bottom: 12px;
  }
  .for-need { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.6; }

  /* PROTOCOL */
  .protocol-section { padding: 100px 0; background: var(--canvas); border-bottom: 1px solid var(--rule); }
  .protocol-inner { max-width: 680px; }
  .timeline { margin-top: 40px; border-left: 1px solid var(--rule); padding-left: 32px; }
  .t-item {
    display: grid; grid-template-columns: 100px 1fr;
    gap: 24px; padding: 0 0 32px;
  }
  .t-time {
    font-size: 10px; font-weight: 600; letter-spacing: 0.1em;
    text-transform: uppercase; color: var(--gold-dark);
    font-family: var(--sans); padding-top: 3px; line-height: 1.4;
  }
  .t-title { font-size: 15px; font-weight: 400; color: var(--ink); margin-bottom: 6px; }
  .t-desc { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.6; }

  /* NEXT */
  .next-section { padding: 80px 0; background: var(--canvas-2); border-top: 1px solid var(--rule); }
  .next-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--rule); border: 1px solid var(--rule); margin-top: 32px; }
  .next-card {
    background: var(--white); padding: 28px;
    text-decoration: none; color: inherit; transition: background 0.2s;
  }
  .next-card:hover { background: var(--canvas-2); }
  .next-id { font-size: 10px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase; color: var(--gold-dark); font-family: var(--sans); margin-bottom: 10px; }
  .next-title { font-family: var(--serif); font-size: 17px; font-weight: 300; color: var(--ink); margin-bottom: 8px; }
  .next-desc { font-size: 13px; font-weight: 300; color: var(--ink-mid); line-height: 1.55; }

  @media (max-width: 900px) {
    .hero-inner { grid-template-columns: 1fr; }
    .hero-right { display: none; }
    .layers-grid { grid-template-columns: repeat(2, 1fr); }
    .for-grid { grid-template-columns: repeat(2, 1fr); }
    .next-grid { grid-template-columns: 1fr; }
    .t-item { grid-template-columns: 80px 1fr; }
  }
  @media (max-width: 640px) {
    .container { padding: 0 24px; }
    .layers-grid { grid-template-columns: 1fr; }
    .for-grid { grid-template-columns: 1fr; }
  }
</style>
